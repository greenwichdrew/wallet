<%= form_for(@debit_card) do |f| %>
  <% if @debit_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@debit_card.errors.count, "error") %> prohibited this debit_card from being saved:</h2>

      <ul>
      <% @debit_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deposits %><br>
    <%= f.text_field :deposits %>
  </div>
  <div class="field">
    <%= f.label :withdraws %><br>
    <%= f.text_field :withdraws %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
